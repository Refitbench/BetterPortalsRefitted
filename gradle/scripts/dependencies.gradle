repositories {
    // Other repositories described by default:
    // CleanroomMC: https://maven.cleanroommc.com
    maven {
        name = 'CurseMaven'
        url = 'https://cursemaven.com'
    }
    maven {
        name = 'CleanroomCurseMaven'
        url = 'https://curse.cleanroommc.com'
    }
    maven {
        name = 'CleanroomMC'
        url = 'https://repo.cleanroommc.com/releases'
    }
    maven {
        name = 'Modrinth'
        url = 'https://api.modrinth.com/maven'
    }
    maven {
        name = 'Outlands'
        url = 'https://maven.outlands.top/releases'
        content {
            includeGroup('org.embeddedt')
            includeGroupAndSubgroups('org.embeddedt.celeritas')
        }
    }
    mavenCentral()
    maven {
        name = 'JitPack'
        url = 'https://jitpack.io'
    }
    mavenLocal() // Must be last for caching to work
}

dependencies {
    if (enable_lwjglx.toBoolean()) {
        modLibrary "com.cleanroommc:lwjglx:1.0.0"
    }

    modCompileOnly ('curse.maven:opencubicchunks-292243:5135427')
    def celeritasVersion = '2.4.0-autobuild.fadd0c40'
    compileOnly("org.embeddedt:celeritas-forge-mc12.2:$celeritasVersion") {
        transitive = false
    }
    compileOnly("org.embeddedt.celeritas:celeritas-common:$celeritasVersion") {
        transitive = false
    }
    // The _Actinium mixin variants bind to Actinium's own renderer namespace (com.dhj.actinium.*).
    // JitPack builds Actinium straight from GitHub (its "Fix JitPack Maven coordinates" commit
    // 34dca21 made its publication coordinates match com.github.DHJComical:Actinium); pin a commit
    // that JitPack has already built and bump it when Actinium-side changes are needed.
    modCompileOnly("com.github.DHJComical:Actinium:${actinium_version}") {
        transitive = false
    }
    // The _Celeritas mixins reference Celeritas' relocated joml namespace, provided by the
    // celeritas-forge jar above (Actinium now ships the same shadow layout at runtime).

    modImplementation ('curse.maven:the-twilight-forest-deforestation-edition-1063334:7846443')

    modImplementation ('curse.maven:mekanism-ce-unofficial-840735:8147536')

    modImplementation ('curse.maven:redstone-flux-270789:2920436')
    modImplementation ('curse.maven:industrial-craft-242638:3838713')

    modImplementation ('curse.maven:aether-255308:6316434')

    modImplementation ('curse.maven:abyssalcraft-53686:7253998')

    modImplementation ('curse.maven:travelhut-255576:2629517')

    modImplementation ('curse.maven:nothirium-624960:7441674')
    modImplementation ('curse.maven:renderlib-624967:6461209')

    modLibrary 'io.github.chaosunity.forgelin:Forgelin-Continuous:2.4.0.0:dev'
    runtimeOnly 'org.jetbrains.kotlin:kotlin-reflect:2.3.21'

    // ========= Integration test dependencies =========

    testImplementation('org.junit.jupiter:junit-jupiter-engine:6.1.0')
    testImplementation('org.junit.platform:junit-platform-launcher:6.1.0')
    testImplementation 'org.jetbrains.kotlin:kotlin-test-junit:2.3.21'
}
